import { Center, Text } from "@chakra-ui/react";
import { DARK } from "@/shared/utils/gatewayColors";

export default function CodingAgentsTab(_props: { days: number; endpoint: string | null }) {
  return <Center py={20}><Text color={DARK.label}>Coding Agents tab — coming soon</Text></Center>;
}
